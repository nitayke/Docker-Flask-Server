# Web server with Flask and Docker

## Installation

### Build container and run it:

`sh start.sh`

### If container exists:

`docker start flask_container`

## Usage

### In the browser (If you need):

`ip:5000/table_name/simulation_id`
